query MyQuery(listId: uuid = "") {
  winnibook_locals_lists(where: {id: {_eq: listId}}) {
    id
    text
    locals {
      localByLocal {
        id
        name
        main_category {
          id
          name
          theme
        }
      }
    }
  }
}
